import './swimmer-globals.css'
import type { ReactNode } from 'react'

export const metadata = { title: 'SwimData — Espace nageur' }

export default function SwimmerLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
